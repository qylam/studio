
{
  "entities": {
    "SharedVision": {
      "title": "SharedVision",
      "description": "A generated AI vision shared by a user from the kiosk.",
      "type": "object",
      "properties": {
        "imageData": {
          "type": "string",
          "description": "The base64 encoded image data URI representing the final Polaroid."
        },
        "theme": {
          "type": "string",
          "description": "The theme selected by the user."
        },
        "activity": {
          "type": "string",
          "description": "The activity chosen or generated for the vision."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "When the vision was created."
        }
      },
      "required": [
        "imageData",
        "activity",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
    "/visions/{visionId}": {
      "schema": "#/backend/entities/SharedVision",
      "description": "Collection of generated visions. Rules: allow read: if true; allow create: if true; // Deployment Trigger: 2024-05-25-16-45"
    }
  }
}
