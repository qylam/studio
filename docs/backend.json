
{
  "entities": {
    "SharedVision": {
      "title": "SharedVision",
      "description": "A generated AI vision shared by a user from the kiosk.",
      "type": "object",
      "properties": {
        "imageData": {
          "type": "string",
          "description": "The base64 encoded image data URI."
        },
        "theme": {
          "type": "string",
          "description": "The theme selected by the user."
        },
        "activity": {
          "type": "string",
          "description": "The activity chosen or generated for the vision."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "When the vision was created."
        }
      },
      "required": ["imageData", "activity", "createdAt"]
    }
  },
  "auth": {
    "providers": ["anonymous"]
  },
  "firestore": {
    "/visions/{visionId}": {
      "schema": "#/backend/entities/SharedVision",
      "description": "Collection of generated visions shareable via unique IDs."
    }
  }
}
